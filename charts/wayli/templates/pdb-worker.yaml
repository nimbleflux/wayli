{{- if and .Values.worker.podDisruptionBudget.enabled (eq .Values.deploymentMode "split") }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "wayli.fullname" . }}-worker
  labels:
    {{- include "wayli.labels" . | nindent 4 }}
    app.kubernetes.io/component: worker
spec:
  {{- if .Values.worker.podDisruptionBudget.minAvailable }}
  minAvailable: {{ .Values.worker.podDisruptionBudget.minAvailable }}
  {{- end }}
  {{- if .Values.worker.podDisruptionBudget.maxUnavailable }}
  maxUnavailable: {{ .Values.worker.podDisruptionBudget.maxUnavailable }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "wayli.selectorLabels.worker" . | nindent 6 }}
{{- end }}
