{{- /*
  SMTP secret is now included in the main fluxbase secret.
  This file is kept for backwards compatibility but will not create a resource.

  SMTP credentials should be provided via:
  - fluxbase.existingSecret with smtpPassword key
  - or fluxbase.config.email.smtp_password (not recommended for production)
*/ -}}
